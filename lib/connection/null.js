var Connection = function() {
  this.psubscribe = function(name) {
    // no op
  }

  this.punsubscribe = function(name) {
    // no op
  }

  this.on = function(evt, on) {
    // no op
  }

  this.quit = function() {
    // no op
  }
};

module.exports = Connection;
